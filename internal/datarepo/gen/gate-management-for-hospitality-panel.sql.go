// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: gate-management-for-hospitality-panel.sql

package datarepo

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getLiveGateLogsQuery = `-- name: GetLiveGateLogsQuery :many
SELECT
  s.name AS student_name,
  s.email AS student_email,
  s.college_name,
  gm.direction,
  gm.logged_at,
  per.name AS personell_name
FROM gate_management gm
LEFT JOIN student s ON gm.student_id = s.id
LEFT JOIN accomodation_personell per ON gm.personell_id = per.id
ORDER BY gm.logged_at DESC
`

type GetLiveGateLogsQueryRow struct {
	StudentName   pgtype.Text          `json:"student_name"`
	StudentEmail  pgtype.Text          `json:"student_email"`
	CollegeName   pgtype.Text          `json:"college_name"`
	Direction     GateLogDirectionEnum `json:"direction"`
	LoggedAt      pgtype.Timestamp     `json:"logged_at"`
	PersonellName pgtype.Text          `json:"personell_name"`
}

func (q *Queries) GetLiveGateLogsQuery(ctx context.Context, db DBTX) ([]GetLiveGateLogsQueryRow, error) {
	rows, err := db.Query(ctx, getLiveGateLogsQuery)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetLiveGateLogsQueryRow{}
	for rows.Next() {
		var i GetLiveGateLogsQueryRow
		if err := rows.Scan(
			&i.StudentName,
			&i.StudentEmail,
			&i.CollegeName,
			&i.Direction,
			&i.LoggedAt,
			&i.PersonellName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getLiveHostelCheckInQuery = `-- name: GetLiveHostelCheckInQuery :many
SELECT
  s.name AS student_name,
  s.email AS student_email,
  s.college_name,
  hm.hostel_name,
  h.checked_in_at AS logged_at,
  per.name AS personell_name
FROM hostel_check_in h
LEFT JOIN accomodation_personell per ON h.checked_in_by = per.id
LEFT JOIN accomodation_details ad ON ad.id = h.accomodation_id
LEFT JOIN hostel_metadata hm ON ad.hostel_id = hm.id
LEFT JOIN student s ON ad.student_id = s.id
ORDER BY h.checked_in_at DESC
`

type GetLiveHostelCheckInQueryRow struct {
	StudentName   pgtype.Text      `json:"student_name"`
	StudentEmail  pgtype.Text      `json:"student_email"`
	CollegeName   pgtype.Text      `json:"college_name"`
	HostelName    pgtype.Text      `json:"hostel_name"`
	LoggedAt      pgtype.Timestamp `json:"logged_at"`
	PersonellName pgtype.Text      `json:"personell_name"`
}

func (q *Queries) GetLiveHostelCheckInQuery(ctx context.Context, db DBTX) ([]GetLiveHostelCheckInQueryRow, error) {
	rows, err := db.Query(ctx, getLiveHostelCheckInQuery)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetLiveHostelCheckInQueryRow{}
	for rows.Next() {
		var i GetLiveHostelCheckInQueryRow
		if err := rows.Scan(
			&i.StudentName,
			&i.StudentEmail,
			&i.CollegeName,
			&i.HostelName,
			&i.LoggedAt,
			&i.PersonellName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
