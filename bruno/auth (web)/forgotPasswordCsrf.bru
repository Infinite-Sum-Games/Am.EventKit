meta {
  name: forgotPasswordCsrf
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/auth/user/forgot-password
  body: none
  auth: none
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Forgot Password (CSRF)
  
  This route gives you the CSRF token required to fill the form for registering as a student on the Anokha website. Without this, your request will be rejected.
  
  ## 200 OK
  ```json
  {
    "message": "",
    "key": "csrf-token-string"
  }
  ```
  The CSRF token must be sent in the subsequent request under the `X-Csrf-Token` header.
  
  ## 500 Internal Server Error
  If you see this, report to the backend team immediately.
  ```json
  {
    "message": "Oops! Something happened. Please try again later."
  }
  ```
}
