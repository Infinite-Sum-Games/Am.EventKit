meta {
  name: logoutUser
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/auth/user/logout
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Logout User
  
  This endpoint cleans up all cookies set by the server. On the client side, please clear any and all stores, local-storage and any other relevant data.
  
  ## 401 Unauthorized
  ```json
  {
    "message": ""
  }
  ```
  
  ## 500 Internal Server Error
  ```json
  ````
}
