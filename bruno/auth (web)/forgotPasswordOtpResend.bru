meta {
  name: forgotPasswordOtpResend
  type: http
  seq: 17
}

get {
  url: {{baseUrl}}/auth/user/forgot-password/otp/resend
  body: json
  auth: inherit
}

body:json {
  {
    "otp": "123456"
  }
}

script:pre-request {
  const jar = bru.cookies.jar();
  
  const path = bru.getEnvVar("baseUrl") + "/auth/user/forgot-password/otp/verify";
  console.log(path);
  
  const cookieValue = await jar.getCookie(path, "csrf_token");
  console.log(cookieValue);
  
  req.setHeader("X-Csrf-Token", cookieValue.value);
}

settings {
  encodeUrl: true
  timeout: 0
}
