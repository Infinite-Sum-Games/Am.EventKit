meta {
  name: verifyUserOtp
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/user/register/otp/verify
  body: json
  auth: inherit
}

body:json {
  {
    "otp": "951749"
  }
}

script:pre-request {
  const jar = bru.cookies.jar();
  
  const path = bru.getEnvVar("baseUrl") + "/auth/user/register/otp/verify";
  console.log(path);
  
  const cookieValue = await jar.getCookie(path, "csrf_token");
  console.log(cookieValue);
  
  req.setHeader("X-Csrf-Token", cookieValue.value);
}

settings {
  encodeUrl: true
  timeout: 0
}
