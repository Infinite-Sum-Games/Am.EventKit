meta {
  name: verifyUserOtp
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/user/otp/verify
  body: none
  auth: inherit
}

script:pre-request {
  const jar = bru.cookies.jar();
  
  const path = bru.getEnvName("baseUrl") + "/auth/user/otp/verify";
  const cookieValue = await jar.getCookie(path, "csrf_token");
  
  req.setHeader("X-Csrf-Token", cookieValue.value);
}

settings {
  encodeUrl: true
  timeout: 0
}
