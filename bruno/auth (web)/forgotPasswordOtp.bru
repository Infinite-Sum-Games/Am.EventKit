meta {
  name: forgotPasswordOtp
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/auth/user/forgot-password/otp/verify
  body: json
  auth: inherit
}

body:json {
  {
    "otp": "123456"
  }
}

script:pre-request {
  const jar = bru.cookies.jar();
  
  const path = bru.getEnvName("baseUrl") + "/auth/user/forgot-password/otp/verify";
  console.log(path);
  
  const cookieValue = await jar.getCookie(path, "csrf_token");
  console.log(cookieValue);
  
  req.setHeader("X-Csrf-Token", cookieValue.value);
}

settings {
  encodeUrl: true
  timeout: 0
}
