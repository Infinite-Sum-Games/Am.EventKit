meta {
  name: CreateEventTag
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/tags
  body: json
  auth: inherit
}

body:json {
  { 
    "name": "ao", 
    "abbreviation": "anokha" 
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
